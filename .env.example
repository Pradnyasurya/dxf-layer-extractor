# DXF Layer Validator Environment Configuration

# Flask Secret Key (change in production!)
# Use a strong random string for production deployments
SECRET_KEY=dev-secret-key-change-in-production

# Database URL (SQLite by default)
# For production PostgreSQL: postgresql://user:password@localhost/dxf_validator
DATABASE_URL=sqlite:///dxf_versions.db

# Flask Environment
# Options: development, production, testing
FLASK_ENV=development

# Debug Mode (disable in production)
FLASK_DEBUG=True

# Upload Settings
MAX_CONTENT_LENGTH=104857600

# Docker Hub credentials (for CI/CD)
# DOCKER_USERNAME=your_username
# DOCKER_PASSWORD=your_password
